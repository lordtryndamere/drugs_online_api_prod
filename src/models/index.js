const userModel = require('../models/users.model');
const roleModel = require('../models/rolle.model');
const dependecyModel = require('../models/dependecies.model');

module.exports = [
    userModel, roleModel, dependecyModel
]