const userModel = require('../models/users.model');
const roleModel = require('../models/rolle.model');
const userTokenModel = require('../models/user-token.model');


module.exports = [
    userModel, roleModel,userTokenModel
]